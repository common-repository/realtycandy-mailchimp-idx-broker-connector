<?php if ( 'wpps_section-basic' == $section['id'] ) : ?>

	<p>Select "yes" if you want the plugin to "listen" to all changes in your Idx
	 user base and auto-sync them with the selected MailChimp list.</p>

<?php elseif ( 'wpps_section-cron' == $section['id'] ) : ?>

	

<?php endif; ?>
